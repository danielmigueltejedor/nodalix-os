#!/usr/bin/env python3
import json
from datetime import datetime

now = datetime.now()
text = now.strftime("%a %d").replace(".", "")

print(json.dumps({
    "text": "󰃭 " + text,
    "tooltip": "Calendario · próximamente",
    "class": "normal"
}))
