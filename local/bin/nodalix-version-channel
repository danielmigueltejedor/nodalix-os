#!/usr/bin/env bash
set -euo pipefail

branch="$(nodalix-version-branch 2>/dev/null || echo local)"

case "$branch" in
  main|stable) echo "stable" ;;
  alpha|beta|dev) echo "$branch" ;;
  *) echo "custom" ;;
esac
