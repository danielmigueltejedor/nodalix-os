#!/usr/bin/env bash
set -euo pipefail

if command -v nodalix-volume-menu >/dev/null 2>&1; then
  exec nodalix-volume-menu
fi

if [ -x "$HOME/Proyectos/nodalix-os/local/bin/nodalix-volume-menu" ]; then
  exec "$HOME/Proyectos/nodalix-os/local/bin/nodalix-volume-menu"
fi

notify-send "Nodalix" "No encuentro nodalix-volume-menu"
