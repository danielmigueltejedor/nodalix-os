#!/usr/bin/env bash
set -euo pipefail

APP="$HOME/Projects/nodalix-os/local/bin/media-popup-window"
LOG="/tmp/nodalix-media-popup.log"

if pgrep -f "$APP" >/dev/null; then
  pkill -f "$APP" || true
  exit 0
fi

nohup "$APP" >"$LOG" 2>&1 &
