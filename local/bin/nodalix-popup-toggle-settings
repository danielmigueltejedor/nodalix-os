#!/usr/bin/env bash
set -euo pipefail

if command -v nodalix-settings-menu >/dev/null 2>&1; then
  exec nodalix-settings-menu
fi

if [ -x "$HOME/Projects/nodalix-os/local/bin/nodalix-settings-menu" ]; then
  exec "$HOME/Projects/nodalix-os/local/bin/nodalix-settings-menu"
fi

notify-send "Nodalix" "No encuentro nodalix-settings-menu"
