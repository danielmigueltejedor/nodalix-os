[package]
name = "nodalix-system-actions"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "System session and power actions for Nodalix OS (lock, suspend, reboot, etc.)"

[dependencies]
