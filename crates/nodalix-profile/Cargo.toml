[package]
name = "nodalix-profile"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Shared Nodalix OS user profile, avatar and visual preferences"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
