[package]
name = "nodalix-assistant-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Local-first assistant core, intents, permissions and context index for Nodalix OS"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
