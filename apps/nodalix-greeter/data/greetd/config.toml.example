[terminal]
vt = 1

[default_session]
# Hyprland warns on the VT if launched as bare "Hyprland"; start-hyprland sets session env.
command = "/usr/bin/start-hyprland -- --config /etc/greetd/hyprland-nodalix-greeter.conf"
user = "greeter"
