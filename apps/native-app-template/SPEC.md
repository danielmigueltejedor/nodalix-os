# Specification

The template provides:

- Rust + GTK4/libadwaita application shell.
- Nodalix app id pattern.
- Dark glass CSS baseline.
- Basic window, header, and empty state.
- Safe run/build scripts.

